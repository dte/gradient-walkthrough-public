clusterId: cltge23zh
command: apt-get update && apt-get install -y libgtk2.0-dev libsm6 libxext6 libxrender-dev && pip install opencv-python && python classify/classify.py
container: pytorch/pytorch:latest
experimentEnv: 
    TRAIN_EPOCHS: 5
    BATCH_SIZE: 64
    NUM_WORKERS: 1
machineType: p2.xlarge
modelType: ONNX
name: classify-singlenode
tensorboard: false